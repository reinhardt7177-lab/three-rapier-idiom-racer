# 프로젝트 작업 원칙

## 기본 태도

- 한국어로 자연스럽게 소통하고, 핵심 결과와 다음 행동부터 설명한다.
- 항상 비판적으로 검토한다. 구현자의 의도보다 플레이어가 실제로 보고 조작하는 결과를 기준으로 판단한다.
- 목표는 결함을 숨기지 않고 발견·수정·재검증하는 것이다. 무결점, 완벽, 모든 기기 지원을 근거 없이 선언하지 않는다.
- 관찰된 사실, 코드로부터의 추론, 제안, 아직 검증하지 못한 내용을 구분한다.

## 변경 전

1. 현재 코드와 사용자의 기존 변경을 확인한다. 과거 문서의 구현 완료 주장을 현재 사실로 단정하지 않는다.
2. 해결할 사용자 문제, 재현 조건, 원인 가설, 수정 범위와 완료 기준을 먼저 정한다.
3. 게임성·시각 디자인·UI/UX·물리/입력·성능·접근성 관점에서 반례와 실패 경로를 찾는다. 실제로 수행하지 않은 독립 전문가 검토를 주장하지 않는다.
4. 작은 검증 가능한 단위로 작업한다. 기능 확장보다 조작 불능·진행 차단·시야 방해·성능 문제 해결을 우선한다.

## 심각도와 진행 기준

| 수준 | 예시 | 대응 |
| --- | --- | --- |
| P0 | 데이터 손실, 비밀 정보 노출, 실행 불가 | 관련 배포를 중단하고 원인·복구 방안을 우선 확인 |
| P1 | 입력 고착, 잘못된 조향, 반복 충돌/추락, 임무 진행 불가 | 관련 콘텐츠 확장 전에 수정·회귀 검증 |
| P2 | 도로 시야 가림, 터치 오조작, 목표 성능 미달 | 지원 기기·사용 빈도에 따라 출시 차단 여부를 명시 |
| P3 | 장식·미세 연출·비핵심 편의성 | 핵심 흐름 검증 뒤 처리 |

미검증 상태는 결함 확정과 다르다. 다만 실기기 검증이 없으면 해당 기기의 지원 완료를 선언하지 않는다.

## 구현과 검증

- 증상을 가리는 숫자 변경이나 테스트 기준 완화로 성공을 만들지 않는다. 속도 표시와 실제 이동, 바퀴 각도와 물리 조향처럼 표현과 동작의 일치를 확인한다.
- 코드 변경에는 관련 자동 시험과 빌드, 사용자 흐름 회귀 검증을 수행한다. 문서만 변경했다면 그 범위에 맞게 링크·내용·diff를 검사한다.
- UI 변경은 가로/세로 화면, 실제 렌더링, 도로 가시성, 입력 충돌, 키보드와 터치를 확인한다. 화면 크기 에뮬레이션을 실기기 테스트로 표현하지 않는다.
- 차량 입력은 동시 가속/조향, 포인터 취소, 메뉴 열기, 화면 회전, 포커스 상실과 복귀 후 고착 여부까지 확인한다.
- 성능은 기기·브라우저·해상도·그래픽 모드·측정 시간을 함께 기록한다. 평균 FPS만으로 끊김과 장시간 안정성을 판단하지 않는다.
- 차고 → 출발 → 주행 → 목표 달성 → 복귀 → 재시작을 연결해서 확인한다. 단일 화면이 정상인 것만으로 게임 전체를 완료 처리하지 않는다.
- 실패하면 재현 조건과 증거를 보존하고 수정 후 같은 조건으로 다시 검증한다. 관련 없는 기존 문제도 발견 사실과 범위를 알린다.

## 보존과 권한

- 사용자 파일·진행 데이터·기존 변경은 함부로 삭제하거나 되돌리지 않는다. 비밀키와 환경 파일을 읽어 출력하거나 커밋하지 않는다.
- 분석·계획 요청은 임의의 게임 수정, 커밋, 푸시, PR 생성/병합, 운영 배포의 허가가 아니다. 각 작업은 사용자가 요청한 범위에서 실행한다.
- 로컬 수정, 검증 결과, GitHub 반영, 운영 배포 상태를 구분해 보고한다.

## 기록과 완료 보고

- 현재 우선순위는 [docs/NEXT_STEPS.md](docs/NEXT_STEPS.md), 세부 구현과 검증은 해당 기능 문서에 기록한다. 오래된 실험 기록은 보존하되 현재 상태와 혼동하지 않도록 표시한다.
- 완료 보고에는 변경 내용, 실제 실행한 검증, 미검증/잔여 위험, 가장 중요한 다음 작업을 포함한다.
- 시험 개수보다 검증 범위와 실패 가능성을 중시한다. 이전 실행 결과를 이번 변경의 재검증 결과로 사용하지 않는다.
