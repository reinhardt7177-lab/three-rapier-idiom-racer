# 레이스 HUD 품질 라운드

- 상단 라이브 상태 아래에 `SECTOR 01 / 03` 진행바를 추가했다.
- 진행바는 현재 랩 내 체크포인트 위치를 기준으로 갱신된다.
- 크루즈 주행 시 `LINE` 보조 조향이 자동 활성화되어 조작 상태가 HUD에도 표시된다.
- `CHASE → BUMPER → DRONE` 카메라 전환과 HUD 위치를 데스크톱에서 확인했다.
- 네온 에이트 출발·크루즈 화면에서 섹터 표시와 카메라 전환을 검증했다.
- 새 브라우저 탭 콘솔 오류 0건을 확인했다.
