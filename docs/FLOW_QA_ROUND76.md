# Flow QA round 76

첫 진입과 화면 전환 회귀를 정적 게이트에 포함했다.

## 보호한 흐름

- 차고지에서 서킷 선택으로 이동
- 서킷 선택에서 물리 프리워밍 후 레이스 시작
- 레이스 종료 콜백에서 결과 화면 전환
- 결과 화면에서 차고지 복귀
- 새 트랙/차량 선택 시 기존 런타임 정리
- 손상되거나 잘못된 `mumu-circuit-best` 저장값을 빈 기록으로 복구

`npm.cmd run check`에 `scripts/check-flow.mjs`를 포함해 전환 마커를 매번 확인한다.
