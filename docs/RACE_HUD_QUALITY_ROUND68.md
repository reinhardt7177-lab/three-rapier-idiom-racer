# 레이스 HUD 퀄리티 라운드 68

- 섹터 리드아웃이 실제 체크포인트 인덱스를 읽도록 연결했다.
- 섹터는 3등분된 트랙 구간으로 계산한다.
- 진행 바는 현재 섹터 내부 0~100%로 표시한다.
- 기존 HUD 업데이트를 감싸는 방식으로 연결해 물리 루프와 UI 상태를 분리했다.
- Visual QA에 `trackProgress` 런타임 마커를 추가했다.

